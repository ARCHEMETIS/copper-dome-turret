# 🚀 เริ่มตรงนี้ — ไฟล์แรกที่ต้องอ่าน

ยินดีต้อนรับสู่โปรเจค Copper Dome ถ้ายังไม่เคยเขียนโปรแกรมจับภาพ (vision) หรือคุมฮาร์ดแวร์มาก่อนเลย ไฟล์นี้จะบอกว่า **"ต้องเปิดไฟล์ไหนตอนนี้"** แบบไม่ต้องเดา

> อย่าเพิ่งไปเปิดไฟล์อื่นในโปรเจค — ทำตามลำดับด้านล่างทีละขั้น พอถึงขั้นนั้นค่อยเปิดไฟล์ที่บอก

---

## 🗺️ แผนที่โฟลเดอร์ (มีอะไรบ้าง ทำไมต้องมีหลายที่)

```
Project III/
├── START-HERE.md   ← ★ อยู่ตรงนี้
├── learn/          ← STEP 1: เรียน vision พื้นฐาน 5 บท (ไม่ต้องมีอุปกรณ์ Arduino)
├── src/            ← STEP 3: โปรแกรมจริงของป้อม (ไปดูตอนเข้าใจ learn/ แล้ว)
├── tools/          ← STEP 2/4: สคริปต์ช่วยทดสอบ/คำนวณ/calibrate
├── docs/           ← อ่านตอนอยากรู้ลึก: แผนงานเต็ม + คู่มือ troubleshoot
└── README.md       ← หน้าสรุปโปรเจค (ไว้โชว์คนอื่น เช่นลง GitHub)
```

**กติกาง่ายๆ**: ตอนนี้ยังไม่มีอุปกรณ์ → อยู่ใน `learn/` กับ `tools/` (โหมดจำลอง) พอได้อุปกรณ์แล้วค่อยไป `src/` ตัวจริง

---

## ✅ ลำดับขั้นที่ต้องทำ

### STEP 1 — เรียน vision ให้เข้าใจก่อน (ตอนนี้เลย)

เปิดไฟล์ `learn/README-บทเรียน.md` แล้วทำตาม จะได้เรียนรู้ทีละเรื่องด้วยโปรแกรมที่รันได้จริง:

1. `learn/01_camera.py` — เปิดกล้อง เข้าใจว่า "ภาพ" ในคอมคืออะไร
2. `learn/02_hsv_tuner.py` — หา "ช่วงสี" ของวัตถุด้วยแถบเลื่อน
3. `learn/03_track_object.py` — ตามวัตถุ + จำลองการเล็ง (หัวใจของป้อม)
4. `learn/04_distance.py` — วัดระยะวัตถุด้วยกล้องตัวเดียว
5. `learn/05_yolo_demo.py` — ลอง AI ตรวจจับวัตถุสำเร็จรูป

ทำทีละไฟล์ ห้ามข้าม อ่านคอมเมนต์ในไฟล์ไปด้วยตอนรัน — เขียนอธิบายไว้ละเอียดแล้ว

**วิธีรัน (ทุกครั้งต้องพิมพ์แบบนี้ ห้ามพิมพ์ `python` เฉยๆ):**
```powershell
cd "C:\Users\Pc\Desktop\Project III"
venv\Scripts\python.exe learn\01_camera.py
```

✅ ผ่าน STEP 1 เมื่อ: ดูทั้ง 5 ไฟล์รันแล้วพอเข้าใจภาพรวมคร่าวๆ ว่า "กล้องเห็นวัตถุ → รู้ตำแหน่ง → รู้ระยะ" ทำได้ยังไง (ไม่ต้องเป๊ะทุกบรรทัด แค่จับ concept ให้ได้)

---

### STEP 2 — ลองระบบเต็มแบบจำลอง (ยังไม่ต้องมีอุปกรณ์)

จะเห็นป้อมทั้งระบบทำงานจริงบนจอ (ป้อมเสมือน + เป้าสุ่มตำแหน่ง):

```powershell
venv\Scripts\python.exe src\main.py --sim
```

เลือกเป้าในหน้าต่าง กด FIRE ดูป้อมกวาดหา เล็งเข้ากลาง วัดระยะ แล้วยิง — โค้ดที่รันคือของจริงทั้งหมด (ไม่ใช่ของเล่นแยกต่างหาก)

✅ ผ่าน STEP 2 เมื่อ: ลองยิงจนเข้าใจว่าปุ่ม/เป้า/สถิติโดน-พลาดในหน้าต่างหมายถึงอะไร

---

### STEP 3 — พอได้อุปกรณ์ Arduino จริง

1. อัปโหลดเฟิร์มแวร์ครั้งเดียว: เปิด Arduino IDE → `File > Examples > Firmata > StandardFirmata` → Upload
2. ทดสอบฮาร์ดแวร์ทีละส่วนด้วยคีย์บอร์ด:
   ```powershell
   venv\Scripts\python.exe tools\test_hardware.py
   ```
3. จากนั้นอ่าน **`docs/คู่มือโค้ด-อ่านก่อน.md`** แบบละเอียด — มีลำดับ calibrate ครบ (โฟกัสกล้อง, ตาราง PWM↔ระยะ, มุม feeder) และตารางแก้ปัญหาที่คนอื่นเจอบ่อย

---

### STEP 4 — อยากรู้ภาพรวมทั้งโปรเจค / แผนเวลา / เกณฑ์คะแนน

อ่าน **`docs/แผนโปรเจค-CopperDome.md`** — มีโจทย์เต็ม, สเปคสนาม, แผนเวลา 3 สัปดาห์, ตารางความเสี่ยง

---

## 🆘 งงว่าต้องเปิดไฟล์ไหน? เช็คตารางนี้

| อยากทำอะไร | เปิดไฟล์นี้ |
|---|---|
| เพิ่งเริ่ม ไม่รู้จะเริ่มตรงไหน | ไฟล์นี้ (START-HERE.md) ← อยู่แล้ว |
| หัดเขียน vision ทีละขั้น | `learn/README-บทเรียน.md` |
| อยากเห็นป้อมทำงานทั้งระบบ (ไม่มีอุปกรณ์) | `venv\Scripts\python.exe src\main.py --sim` |
| ทดสอบว่ามอเตอร์/servo ต่อถูกไหม | `venv\Scripts\python.exe tools\test_hardware.py` |
| วิธี setup/calibrate/แก้ปัญหาแบบละเอียด | `docs/คู่มือโค้ด-อ่านก่อน.md` |
| แผนงาน/เกณฑ์คะแนน/timeline | `docs/แผนโปรเจค-CopperDome.md` |
| อยากรู้ว่าโค้ดแต่ละไฟล์ใน src/ ทำหน้าที่อะไร | `README.md` หัวข้อ "โครงสร้างโค้ด" |

ไม่แน่ใจ ให้ถามได้เสมอว่า "ตอนนี้ควรทำอะไรต่อ" แล้วบอกว่าทำถึง step ไหนแล้ว
