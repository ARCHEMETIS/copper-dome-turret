# =============================================================
# config.py — ค่าคงที่ทั้งหมดของระบบ รวมไว้ที่เดียว
# กติกา: ไฟล์อื่นห้าม hardcode ตัวเลข ให้ import จากที่นี่เท่านั้น
# ค่าที่ขึ้นต้นด้วย TODO ต้องวัด/จูนจากของจริงก่อนใช้
# =============================================================
import hashlib
from functools import lru_cache
from pathlib import Path

_PROJECT_ROOT = Path(__file__).resolve().parent.parent

# ---------- Serial / Arduino ----------
SERIAL_PORT = None          # None = ค้นหาอัตโนมัติ, หรือระบุเอง เช่น "COM4"
                            # ดูพอร์ตได้ใน Device Manager > Ports (COM & LPT)
                            # เครื่องหลัก: Uno โผล่เป็น COM7 (ชิป CH340) — autodetect เจอเอง
                            # แต่ถ้าวันแข่งมีอุปกรณ์ serial อื่นเสียบอยู่ ให้ล็อกเป็น "COM7"

# ---------- กลไกยิง: เลือกได้ ----------
# 20 ก.ค. 2026: เพื่อน (คนดูแลกลไก) ทิ้งหน้าไม้ กลับไปใช้ flywheel ที่ทดสอบแล้วว่า
# ยิงแรง วิถีแบนมาก และไกล → ค่าเริ่มต้นกลับเป็น "flywheel"
# เก็บโค้ดหน้าไม้ไว้ครบ สลับกลับได้ด้วยบรรทัดเดียว (กลไกเปลี่ยนมา 2 รอบแล้ว เผื่อเปลี่ยนอีก)
# รูปแบบเดียวกับ DETECTOR = "yolo"/"hsv" ด้านล่าง
#
#   "flywheel" — ล้อ 2 ตัวหมุนสวนกันหนีบลูกออกไป ป้อนลูกด้วยมือ/แรงโน้มถ่วง (ไม่มี feeder servo)
#   "crossbow" — มอเตอร์ดึงสายผ่านเฟืองครึ่งวง ยางดีดเอง จับจังหวะด้วย cam_switch
LAUNCHER = "flywheel"

# ---------- ขา Arduino (Uno + Sensor Shield v5) ----------
# ทั้งสองกลไกใช้ "ชุดสายเดิมทั้งหมด" (L298N + มอเตอร์ DC 2 ตัว + servo 2 ตัว) ไม่ซื้อเพิ่ม
# ต่างกันแค่ "ทิศทางมอเตอร์" กับ "มี cam_switch ไหม" — ดู hardware.py
PIN_PAN_SERVO = 9           # MG945 ตัวที่ 1 หมุนป้อมซ้าย-ขวา
PIN_TILT_SERVO = 10         # MG945 ตัวที่ 2 คุมมุมเงย ผ่านเฟืองสะพาน (rack) ใต้ตัวป้อม
                            # ⚠ ตัวนี้เคยเป็น feeder servo สมัย flywheel รอบแรก — รอบนี้ยกให้ tilt
                            # เพราะป้อมอยู่ "ต่ำกว่า" ตุ๊กตา มุมเงยที่ต้องใช้จึงเปลี่ยนตามระยะจริง
                            # (เป้าสูงกว่าปากกระบอก 0.5 ม. → 18.4° ที่ 1.5 ม. แต่ 11.8° ที่ 2.4 ม.
                            #  = ต่าง 6.6°) วิถีแบนแก้แค่ "ลูกตก" ไม่ได้แก้มุมเล็งที่เปลี่ยนตามระยะ
                            # → ลูกจึงป้อนด้วยมือแทน ไม่เสีย tilt ไปทำ feeder
PIN_MOTOR_ENA = 5           # PWM -> ENA ของ L298N (มอเตอร์ตัวที่ 1: ล้อซ้าย / ดึงสาย)
PIN_MOTOR_ENB = 6           # PWM -> ENB ของ L298N (มอเตอร์ตัวที่ 2: ล้อขวา / ดึงสาย)
PIN_IN1 = 2                 # ทิศทางมอเตอร์ A (ตั้งครั้งเดียวตอนเปิดเครื่อง ตาม LAUNCHER)
PIN_IN2 = 4
PIN_IN3 = 7
PIN_IN4 = 8
# ทิศทางมอเตอร์ต่างกันตามกลไก — hardware.py ตั้งให้เองตาม LAUNCHER:
#   flywheel : สองล้อหมุน "สวนกัน" เพื่อหนีบลูกออกไปข้างหน้า
#   crossbow : สองตัวหมุน "ทิศเดียวกัน" เพื่อดึงสาย/เฟืองครึ่งวงเข้าหาตัว
# ถ้าหมุนผิดทาง (ลูกถูกดูดเข้าแทนที่จะพุ่งออก / ดึงสายไม่เข้า) ให้สลับ 1/0 ของคู่ IN ตัวนั้น
PIN_CAM_SWITCH = 12         # ⚠ ใช้เฉพาะโหมด crossbow เท่านั้น — โหมด flywheel ไม่ต่อ ไม่อ่าน
# สวิตช์โฮมเมด (ไม่ซื้อของเพิ่ม): ลวด/แผ่นโลหะเล็กๆ ติดกับเฟืองดึง แตะสาย GND แค่ตอนอยู่ตำแหน่ง
# "พร้อมยิง" (จุดที่เฟืองครึ่งวงหลุด/ดีดออกไปแล้ว หมุนกลับมาสุด) — 1 จุดสัมผัสต่อ 1 รอบเฟือง
# ใช้จับจังหวะแทนการนับเวลา เพราะความเร็วมอเตอร์ไม่คงที่ (ไฟตกตอนออกตัว เจอมาแล้วกับ flywheel 15 ก.ค.)

# ---------- Servo: ขอบเขตมุม ----------
PAN_CENTER = 90
PAN_MIN = 40                # กันสายพันคอป้อม (แผนคือ ±40°)
PAN_MAX = 140

# ---------- Tilt (มุมเงย — แกนเล็งแนวตั้ง "โหมดสโคป") ----------
# ตัดสินใจ 18 ก.ค. (รอบสอง): ยิงแรงคงที่ทุกนัด วิถีแบน ไม่คุมระยะด้วยมุมอีกแล้ว —
# tilt กลายเป็นแกน "เล็ง" เหมือน pan: กล้องติดไปกับลำกล้อง (สโคป) เล็งเป้าเข้าจุด zero
# บนจอแล้วยิงเลย ไม่ต้องวัดระยะ ไม่มีตารางมุม (ดู docs/แผนโปรเจค-CopperDome.md §2.5)
# TODO: ขอบเขตมุมต้องวัดจากของจริงหลังประกอบเฟือง rack เสร็จ (กันเฟืองชนกัน/servo หลุด)
TILT_CENTER = 35            # มุมเริ่มต้น/พัก
TILT_MIN = 20               # มุมเงยต่ำสุดที่เฟือง rack ไปได้ก่อนชน
TILT_MAX = 60               # มุมเงยสูงสุดก่อนเฟือง rack หลุดจากกัน

# ---------- ล้อยิง flywheel (LAUNCHER = "flywheel") ----------
# ยิงแรงคงที่ทุกนัดเหมือนกัน ไม่ผันตามระยะ (โหมดสโคป) — ระยะจัดการด้วยการเล็ง ไม่ใช่ความแรง
FLYWHEEL_DUTY = 1.0          # TODO: ยืนยันกับเพื่อนว่าตอนทดสอบใช้ duty เท่าไร ที่ไฟกี่โวลต์
                             # เพื่อนรายงานว่า "แรงมาก วิถีแบนมาก ไกล" → เริ่มที่เต็มกำลังไว้ก่อน
FLYWHEEL_MIN_DUTY = 0.3      # ต่ำกว่านี้ล้อมักไม่หมุน (แรงเสียดทานชนะแรงบิด)
FLYWHEEL_MAX_DUTY = 1.0
FLYWHEEL_SPINUP_S = 1.5      # TODO: วัดจริง — เวลาที่ล้อต้องใช้จนความเร็ว "นิ่ง" พอให้ทุกนัดเท่ากัน
                             # ป้อนลูกก่อนล้อนิ่ง = นัดนั้นเบากว่าเพื่อน จุดตกจะเพี้ยนจาก zero
FLYWHEEL_FEED_WINDOW_S = 2.5 # หน้าต่างเวลาที่ล้อค้างความเร็วเต็มไว้ให้คนหย่อนลูกลงราง
                             # (ไม่มี feeder servo — servo ตัวที่ 2 ไปเป็น tilt แล้ว)
                             # TODO: ปรับตามความถนัดจริงตอนซ้อม สั้นไปจะป้อนไม่ทัน ยาวไปเปลืองแบต+ล้อร้อน
# soft-start: ไต่ duty ขึ้นทีละขั้นแทนการกระแทก 0→เต็ม — มอเตอร์ออกตัวกระชากกระแสหลายเท่า
# ทำแบตวูบจน L298N ดับทั้งบอร์ด (เจอของจริง 15 ก.ค. กับ flywheel ชุดนี้เอง)
FLYWHEEL_RAMP_STEP = 0.05    # ขยับทีละกี่ duty ต่อขั้น
FLYWHEEL_RAMP_STEP_S = 0.05  # หน่วงต่อขั้น — ลงทันทีไม่ต้อง ramp

# ---------- มอเตอร์ดึงสาย (LAUNCHER = "crossbow" — เก็บไว้เผื่อสลับกลับ) ----------
DRAW_DUTY = 0.8              # TODO: ความแรงตอนดึง (ค่าคงที่เดียว ใช้ทุกนัด ไม่ผันตามระยะ) —
                             # วัดจริงว่าดึงเฟืองผ่านจนหลุดลื่นไม่สะดุด ต่ำกว่า ~0.5 มอเตอร์มักดึง
                             # เฟืองไม่ไหว (แรงยาง) TODO: วัดจริง
DRAW_RAMP_STEP = 0.05        # ขยับทีละกี่ duty ต่อขั้น (tools/test_catapult.py ใช้ค่านี้ด้วย)
DRAW_RAMP_STEP_S = 0.05      # หน่วงต่อขั้น — ลงทันทีไม่ต้อง ramp
DRAW_MAX_DUTY = 1.0
DRAW_TIMEOUT_S = 4.0         # TODO: ปรับตามความเร็วมอเตอร์จริง — กันสวิตช์เสีย/สายหลุดแล้วมอเตอร์หมุนไม่หยุด

# ---------- จุด zero ของสโคป ----------
# offset (x, y) เป็น pixel จากกลางจอ = จุดบนภาพที่ "ถ้าเป้าอยู่ตรงนี้ = ยิงโดน"
# วิธี zero (ทำครั้งเดียวหลังประกอบ): ยิงจริง 1 นัดที่ระยะกลางสนาม (~2m) → ดูว่าลูก
# ไปโดนจุดไหน → เลื่อนจุด zero ใน main_click.py ด้วยปุ่ม I/J/K/L ให้ทับจุดที่โดนจริง
# → จดค่าที่โชว์มาใส่ตรงนี้ (เป็น list ไม่ใช่ tuple เพราะ UI ขยับค่าตอนรันได้)
# วิถีแบน + เป้าทุกตัวสูงเท่ากัน (โต๊ะเดียวกัน) → จุดเดียวใช้ได้ทุกระยะ 1.5-2.4m
# แต่ต้องยืนยันด้วยการยิงตัวใกล้สุด/ไกลสุดหลัง zero — ถ้าตัวไกลตกต่ำ = ยางแรงไม่พอ
SCOPE_ZERO_OFFSET_PX = [0, 0]

# ---------- กล้อง ----------
CAMERA_INDEX = 1            # 1 = Camo (มือถือ) | 0 = กล้องโน้ตบุ๊ก | None = ไล่หาอัตโนมัติ
FRAME_WIDTH = 1280
FRAME_HEIGHT = 720

# focal length เป็น pixel — fit จากรูปชุด Distance/ ด้วย tools/fit_focal.py
# สูตร: ระยะ_mm = FOCAL_PX * ขนาดจริง_mm / ขนาดใน_ภาพ_px
# ⚠ ค่านี้สเกลมาจากรูปโหมดถ่ายภาพ iPhone (4064px → 1280px) — FOV โหมดรูปกับ
# โหมดวิดีโอ Camo อาจไม่เท่ากัน TODO: ถ่ายผ่าน Camo ที่ระยะที่รู้ 1-2 รูปเพื่อยืนยัน
# (ล็อก focus/zoom มือถือแล้วห้ามแตะอีก ไม่งั้นค่านี้เพี้ยนทั้งระบบ!)
FOCAL_PX = 1400

# ---------- เป้า 3 ตัว ----------
# real_size_mm = ขนาด "ผลจริง" √(กว้าง·สูง) ที่กรอบ YOLO เห็น — fit จากรูปชุด
# Distance/ ด้วย tools/fit_focal.py — ranging ใช้ค่านี้ (√(w·h) ทนท่าวางแปลกๆ
# วันแข่งได้ดีสุด: หมุนตุ๊กตาแล้ว w↔h สลับกันแต่ √(w·h) แทบไม่เปลี่ยน)
# real_width_mm = ความกว้างผลจริง (fit เช่นกัน) — เหลือไว้ให้ simulator วาดเป้า
# ค่าพวกนี้ยังใช้ได้แม้ FOCAL_PX จะถูกปรับตอนยืนยันผ่าน Camo
# (FOV เปลี่ยน = px ทุกอย่างสเกลเท่ากัน แก้ที่ FOCAL_PX ตัวเดียวพอ)
# คาลิเบรตกับโมเดลรอบ 3 แล้ว (10 ก.ค. — Distance/ranging_log.csv 42 แถว:
# 2 ระยะ 100/175cm × 7 ท่า × 3 ตัว, median ของขนาดโดยนัย size_px·dist/FOCAL)
# ความสอดคล้องข้ามระยะดีมาก (ต่างกัน <2%) = ยืนยัน FOCAL_PX 1400 ไปในตัว
# ⚠ ถ้าเทรนโมเดลใหม่อีกรอบ ต้องเก็บซ้ำด้วย tools/collect_ranging_data.py
TARGETS = {
    "dino": {
        "display": "ไดโนเสาร์เขียว",
        "real_size_mm": 171,           # median 14 จุด 2 ระยะ×7 ท่า (ranging_log.csv)
        "real_width_mm": 145,
        "yolo_class": 1,               # ตรงกับลำดับ class ตอนเทรน YOLO (data.yaml: capybara,dino,elephant)
        "hsv_lower": (35, 80, 60),     # ช่วงสีเขียว (สำรอง ถ้า YOLO ไม่ทัน)
        "hsv_upper": (85, 255, 255),
    },
    "capybara": {
        "display": "คาปิบาร่า",
        "real_size_mm": 138,           # median 14 จุด 2 ระยะ×7 ท่า (ranging_log.csv)
        "real_width_mm": 118,
        "yolo_class": 0,
        "hsv_lower": (10, 60, 60),     # น้ำตาล — เสี่ยงชนกับช้าง ควรใช้ YOLO
        "hsv_upper": (25, 255, 255),
    },
    "elephant": {
        "display": "ช้างเทา",
        "real_size_mm": 161,           # median 14 จุด 2 ระยะ×7 ท่า (ranging_log.csv)
        "real_width_mm": 185,
        "yolo_class": 2,
        "hsv_lower": (0, 0, 40),       # เทา — HSV แยกยากมาก ควรใช้ YOLO
        "hsv_upper": (180, 40, 200),
    },
}

# ตัวแก้ระยะตามท่า (ranging.py ใช้): {toy: [(aspect, factor), ...]} = จุด knot
# aspect = w/h ของกรอบ YOLO — บอก "ท่า" ได้: เช่น dino ยืนตรง w/h~0.65,
# หันข้าง ~1.0 (เงาใหญ่ อ่านใกล้เกิน -20% → คูณ ~1.25 ชดเชย)
# ranging ใช้ np.interp ระหว่าง knot จึงต่อเนื่อง — เดิมเป็นแถบ w/h ตายตัว
# ซึ่งมีหน้าผาตรงขอบแถบ (factor กระโดด 14-24%) ทั้งที่ข้อมูลจริงมีจุดนั่ง
# ห่างขอบแค่ ~3% = กรอบสั่นไม่กี่ px ตอนยิงจริงก็ข้ามแถบ ระยะเด้งทั้งนัด
# fit จาก Distance/ranging_log.csv ด้วย tools/fit_aspect.py (โมเดลรอบ 3, 42 จุด)
# หลังแก้: แย่สุด ±10.6% จากเดิม ±21.9% — ตัวเลข in-sample! เช็คของจริงด้วย
# tools/collect_ranging_data.py ที่ระยะที่สาม (ที่ไม่ได้ใช้ fit) ก่อนเชื่อสนิท
# ⚠ ผูกกับโมเดล+ตุ๊กตาชุดนี้ ถ้าเทรนใหม่/เปลี่ยนตุ๊กตา ต้อง fit ใหม่พร้อม real_size_mm
ASPECT_CORRECTION = {
    "capybara": [(0.771, 1.008), (0.83, 1.057), (0.995, 0.89), (1.061, 0.872)],
    "dino": [(0.646, 1.009), (0.765, 0.899), (1.0, 1.251), (1.164, 0.933),
             (1.382, 0.98)],
    "elephant": [(0.905, 1.021), (0.989, 1.067), (1.076, 0.867), (1.13, 0.928),
                 (1.335, 0.863), (1.448, 0.917)],
}

# ---------- Vision ----------
DETECTOR = "yolo"           # "yolo" หรือ "hsv" — สลับเป็น yolov8s+aug 13 ก.ค. + จูน YOLO_CONF 0.5→0.30 ให้เข้าจุดทำงาน
                            # (ที่จุดทำงานเดิม 0.5 recall capybara ตก 0.94→0.88 เพราะโมเดลนี้ "เย็นกว่า" — ดู YOLO_CONF)
                            # ⚠ real_size_mm/ASPECT_CORRECTION ด้านบนยัง fit จากรอบ 3 — ranging ยังไม่ถูกจนกว่าจะเก็บ+fit ใหม่
# ผูกกับตำแหน่งโปรเจคเสมอ (ไม่ใช่ cwd) — ไม่งั้นรันจากโฟลเดอร์อื่นแล้วหาไฟล์ไม่เจอ
YOLO_MODEL_PATH = str(_PROJECT_ROOT / "models" / "best.pt")


@lru_cache(maxsize=1)
def yolo_model_tag() -> str:
    """ป้ายประจำไฟล์โมเดล = ชื่อ + hash สั้น เช่น "best.pt@1a2b3c4d"
    ใช้แท็กแถวใน Distance/ranging_log.csv — ชื่อไฟล์เฉยๆ แยกโมเดลไม่ได้
    เพราะเทรนกี่รอบก็ copy ทับมาเป็น best.pt เหมือนกันหมด แต่ข้อมูล ranging
    ข้ามโมเดลปนกันไม่ได้ (นิสัยการตีกรอบของแต่ละโมเดลไม่เหมือนกัน)"""
    p = Path(YOLO_MODEL_PATH)
    return f"{p.name}@{hashlib.sha1(p.read_bytes()).hexdigest()[:8]}"
YOLO_CONF = 0.30           # v8s+aug "เย็นกว่า" รอบ 3 (maxconf ผี 0.31 vs 0.71) → จูนลงจาก 0.5 ให้เข้าจุดทำงาน
                            # ที่ 0.30: recall เต็ม (capybara คงที่ 0.92 ตั้งแต่ 0.30 ลงไป, dino/elephant ชนะรอบ 3) +
                            # ghost 1/176 บน NegativeDataSet/eval (< รอบ3 2/176) เฟรมเดี่ยว → ประตูเวลากินทิ้ง
                            # ⚠ อย่าขึ้นเกิน 0.32 — capybara ท่ายาก conf ต่ำจริง จะเริ่มตกที่ 0.35 (48→46/52)
HSV_MIN_AREA_PX = 800       # กรอง noise เล็กๆ ทิ้ง
# imgsz สำหรับ detect_all (จอมอนิเตอร์ real-time) — เล็กกว่า default 640 ให้เร็วขึ้น
# ~10ms/เฟรม (47→37) โดยกล่องแทบไม่ต่าง (test 80 รูป: 96 vs 95) ต่ำกว่า 512 ไม่เร็ว
# ขึ้น (ติดเพดาน overhead ต่อ call) แต่เริ่มเสี่ยงพลาด detection. aiming ยังใช้ 640
YOLO_DISPLAY_IMGSZ = 512
# conf สำหรับ detect_all (จอมอนิเตอร์) — ต่ำกว่า YOLO_CONF หลัก เพราะ detection
# ตรงนี้เป็นแค่ "ตัวช่วยเล็ง" ให้คนดู (คลิกเล็งเอง ไม่ได้ใช้ตัดสินยิง) ghost บนจอ
# ไม่อันตราย. ลดให้เป้าที่ conf ต่ำ (เช่นโดนโต๊ะบังท่อนล่าง) มีโอกาสโผล่กรอบให้เห็น
# ⚠ ไม่กระทบ ghost gate / การเล็งอัตโนมัติใน main.py ที่ยังใช้ YOLO_CONF=0.30
YOLO_DISPLAY_CONF = 0.20

# ---------- ประตูกัน ghost (เฉพาะ YoloDetector — HSV/sim ไม่เกี่ยว) ----------
# หลักฐาน+เหตุผลอยู่ docs/vision-baseline.md ข้อ 4 และ wayfinder #10:
# ghost บนคลิป negative อยู่ทนสุด 233ms แต่ตุ๊กตาจริงเจอ 96% ของเฟรม
GATE_PERSIST_FRAMES = 3         # เห็นสะสมกี่เฟรม (นับ +1/-1) ก่อนยอมปล่อย detection
GATE_DIST_RANGE_MM = (500, 4500)  # ระยะที่เป็นไปได้ → ขอบเขตขนาดกรอบผ่านสูตร ranging
                                  # (ขอบไกล 4.5m: เคยตั้ง 3.5m แล้วตาบอดใส่ capybara
                                  # ตัวจริงใน VideoForTest ที่ ~4m — สนามจริง 1.5-2.4m
                                  # แต่ตอนซ้อม/กวาดหาเป้า เจอไกลกว่านั้นได้จริง)

# ---------- เล็งด้วยการคลิก (main_click.py) ----------
# วันแข่งโมเดลอาจมองไม่เห็น/กรอบเพี้ยน คนเลือกเป้าเองอยู่แล้ว จึงมีโหมดสำรอง
# "คลิกที่ไหนก็หันไปที่นั่น" ไม่พึ่ง detection: หมุน pan+tilt เอาพิกเซลที่คลิก
# มาที่จุด zero ของสโคป (มุม = atan(offset_px / FOCAL_PX) ทั้งสองแกน)
CLICK_AIM_GAIN = 1.0        # คูณมุมที่หมุนต่อคลิก (<1 = ค่อยๆ เข้า กันเหวี่ยงเกินถ้า FOCAL_PX ไม่เป๊ะ)

# ---------- Visual servoing (การเล็ง — ใช้โดย main.py/aiming.py แบบอัตโนมัติ) ----------
# โหมดสโคป: เล็ง 2 แกนพร้อมกัน — pan เอาเป้าเข้าแนวตั้งของจุด zero, tilt เอาเข้าแนวนอน
AIM_DEADBAND_PX = 15        # เป้าห่างจุด zero ไม่เกินนี้ (ทั้งสองแกน) = ถือว่าเล็งตรงแล้ว
AIM_KP = 0.03               # องศาที่หมุนต่อ 1 pixel ของ error (ใช้ทั้ง pan และ tilt จูนถ้าส่าย/ช้าไป)
AIM_MAX_STEP_DEG = 6        # หมุนทีละไม่เกินกี่องศา (กันเหวี่ยงเกิน)
AIM_SETTLE_S = 0.20         # รอป้อม+ภาพนิ่งหลังหมุนแต่ละครั้ง
AIM_CONFIRM_FRAMES = 3      # ต้องอยู่ที่จุด zero ติดกันกี่เฟรมถึงยอมยิง
AIM_TIMEOUT_S = 12          # หาเป้าไม่เจอ/เล็งไม่เข้าภายในเวลานี้ = ยกเลิก
AIM_SIGN = +1               # ถ้าป้อมหมุน "หนี" เป้าแนวนอน ให้เปลี่ยนเป็น -1
AIM_TILT_SIGN = +1          # ถ้าป้อมเงย "หนี" เป้าแนวตั้ง ให้เปลี่ยนเป็น -1
AIM_FLUSH_FRAMES = 2        # ทิ้งเฟรมค้างใน buffer กี่เฟรมก่อน detect (กัน servo เล็งจากภาพ "ก่อนหมุน")
                            # 0 = ปิด (ถ้าจูนบนกล้องจริงแล้วรู้สึกหน่วง/ไม่ช่วย), เพิ่มถ้ายังส่ายตามภาพเก่า
AIM_CONFIRM_POLL_S = 0.05   # หน่วงสั้นๆ ระหว่างนับเฟรมยืนยัน (เป้านิ่งในกรอบ deadband แล้ว รอให้ครบ AIM_CONFIRM_FRAMES)
