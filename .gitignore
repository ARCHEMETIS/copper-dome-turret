# Python
venv/
__pycache__/
*.pyc

# ไฟล์ใหญ่ / ผลลัพธ์ระหว่างทาง — ไม่ควรอยู่ใน git
dataset/
models/*.pt
!models/best.pt
yolov8n.pt
yolov8s.pt
yolo26n.pt
runs/
snap_*.jpg
*.log
*.mp4

# IDE / OS
.vscode/
.idea/
Thumbs.db
desktop.ini

# Jupyter (learn/notebooks/) — checkpoint ชั่วคราว + โมเดล YOLO ที่โหลดมาตอนรันบทที่ 5
.ipynb_checkpoints/
learn/notebooks/yolov8n.pt

# สไลด์อาจารย์ - ลิขสิทธิ์ผู้สอน ไม่เผยแพร่
Introduction and mini project 1.pdf

# ไฟล์ 3D จาก Printables/Thingiverse - ลิขสิทธิ์ผู้ออกแบบ ให้โหลดจากลิงก์ใน README แทน
Model/

# วิดีโอดิบจากมือถือ (ใหญ่เกิน git — เนื้อหาถูกสไลซ์เป็นเฟรมเข้า dataset แล้ว)
New_Video/

# เฟรม negative ดิบ — มีใบหน้าเพื่อนร่วมทีม ห้ามขึ้น repo (สำเนาใช้งานอยู่ใน dataset/ ซึ่ง ignore อยู่แล้ว)
NegativeDataSet/

# ไฟล์ lock ชั่วคราวของ Excel
~$*

# base weights ที่โหลดมาเทรน (ใหญ่ + auto-download เองได้)
yolo11s.pt
yolov8m.pt

# Claude Code local settings (per-machine — subagent def + permissions)
.claude/

# ไฟล์ส่วนตัว / รายงาน session — เก็บ local ไม่ขึ้น public repo
RESUME-คืนนี้.md
OVERNIGHT-2026-07-22.md
